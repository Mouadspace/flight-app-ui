import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import styles from './style';

const Home = () => {
  return (
    <SafeAreaView>
      <Text>hello mouad</Text>
    </SafeAreaView>
  );
};

export default React.memo(Home);
